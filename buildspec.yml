version: 0.2

env:
  variables:
    RAILS_ENV: "development"
            
phases:
  install:
    commands:
      - gem install bundler
  build:
    commands:
      - bundle exec rspec
      - bundle exec cucumber
