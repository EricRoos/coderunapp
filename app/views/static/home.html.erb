<% if Current.user %>
  <%= Current.user.email %>
  <h2>Message Center</h2>
  <% Current.user.messages.each do |message| %>
    <%= message.content %>
  <% end %>
<% else %>
  <h1>CodeRun</h1>
<% end %>
